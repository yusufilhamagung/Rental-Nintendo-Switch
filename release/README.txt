Production bundle created in: release

frontend/: static assets (deploy to any static host)
express-api/: Node API (run with: npm start)

To run locally:
  cd release/express-api
  copy .env.example .env  # then adjust DB config
  npm start
